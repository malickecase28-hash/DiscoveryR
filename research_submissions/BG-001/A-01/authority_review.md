# BG-001 / A-01 authority review

Scope: `bar_dependency_lineage` for the canonical XAUUSD bar physical schema and lineage authority limits.

The manifest declares research base `4cf641369f64255624349696c9937eb56cb7dbcc`. The manifest and mount-attestation hashes were checked; the attestation matches the declared repository-source hashes and authority-source identity. Evidence was limited to the manifest-declared repository surfaces and the authorized source ID.

Direct evidence supports the seven registered bar scales, the physical bar fields and types, the physical time-coordinate field names, and the serialized payload surface names. It does not establish lifecycle meaning, producer raw inputs, upstream detector inputs, object IDs, occurrence/known/availability timing, persistence, transitions, causality, scientific interpretation, pair relationships, or confluence. Serialized detector outputs were not classified as raw inputs. Graph edges are intentionally empty.

The candidate is therefore fail-closed: only directly evidenced physical surfaces are AUTHORITATIVE; all semantic and dependency topics remain UNRESOLVED.
